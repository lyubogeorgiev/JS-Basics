function petShop(input){
    let dogFoodAmount = Number(input[0]);
    let catFoodAmount = Number(input[1]);

    let finalAmount = dogFoodAmount * 2.5 + catFoodAmount * 4;

    console.log(finalAmount);
}